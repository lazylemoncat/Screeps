interface CreepMemory {
  role?: string,
  dying?: boolean,
  sourcesPosition?: any,
  harvesting?: boolean,
  upgrading?: boolean,
  building?: boolean,
  transfering?: boolean,
  repairing?: boolean,
}